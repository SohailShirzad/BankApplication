@using myBankApplication.ViewModels
@model UserDetailViewModel
@{
    Layout = "~/Views/Shared/SideNav.cshtml";
}
<!DOCTYPE html>
<html>
<head>
    <link rel="stylesheet" href="~/css/sideNav.css">
    <link rel="stylesheet" href="~/css/home.css">
    <link rel="stylesheet" href="~/css/header.css">
    <link rel="stylesheet" href="~/css/BankCardPage.css">
    <script src="https://kit.fontawesome.com/6703c9cc10.js" crossorigin="anonymous"></script>


</head>
<body>
    <div class="content">
        @foreach (var bankcards in Model.BankCards)
        {
            <div class="circles">
                <div class="circle circle-1"></div>
                <div class="circle circle-2"></div>
            </div>
            <div class="card-group">
            <div class="card">
                <div class="logo"><img src="~/images/visaLogo.png" alt="Visa"></div>
                <div class="chip"><img src="~/images/chipImage.png" alt="chip"></div>
                <div class="number">@bankcards.CardNumber</div>
                    <div class="name">@Model.FName[0].ToString().ToUpper() @if (Model.MName != null)
                        {
                            @Model.MName[0].ToString().ToUpper()
                        }
                        @Model.LName @bankcards.Account_Id</div>
                    <div class="from">EXP @bankcards.ExpiryDate.ToString("MM/yyyy")</div>
                <div class="to"> @bankcards.CVVNumber</div>
                <div class="pin"> PIN: @bankcards.CardPin <a asp-controller="AppUsers" asp-action="EditCardPin" style=" color: skyblue">Change Pin?</a></div>
                <div class="ring"></div>
            </div>

        </div>


        }
</div>
</body>
</html>